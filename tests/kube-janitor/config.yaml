slack:
  channel: "#bots"

resources:
  ns:
    whitelist:
      - default
      - kube-system
      - kube-public
      - kube-node-lease
      - core
      - infra
    annotation_whitelist:
      - fluxcd.io